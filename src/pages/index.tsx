import Layout from "@/components/layout";
import { Box, useTheme } from "@mui/material";

const Home = () => {
  const theme = useTheme();
  return (
    <Layout>
      <Box>salam</Box>
    </Layout>
  );
};

export default Home;
